import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.groceryease.choco',
  appName: 'Chocomart28',
  webDir: 'mobile-shell',
};

export default config;
